// include source files here... 
import { /*{{title_short}}*/ } from '../{{title_short}}.js';

describe('{{title_short}}', function() {

    it('should return the correct value', function() {
        const someInput = 'foo'
        expect(/*{{title_short}}*/(someInput)).toBe(42);
    });

});